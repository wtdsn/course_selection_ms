const dbOption = {
  host: 'localhost',
  user: 'root',
  password: '123456',
  database: 'course_selection'
}

const serverHost = 'http://localhost'
const serverPort = 3000

module.exports = {
  dbOption,
  serverHost,
  serverPort
}